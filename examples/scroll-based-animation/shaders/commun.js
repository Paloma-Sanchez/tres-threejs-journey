import * as THREE from 'three'

/**
 * THREE JS
 * */
//Commun shader geometry
export const shaderGeometry = new THREE.PlaneGeometry(0.3, 0.5, 32, 32)

//Commun number material
export const numberMaterial = new THREE.MeshNormalMaterial()